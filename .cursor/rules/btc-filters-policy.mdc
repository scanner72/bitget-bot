---
description: BTC filters — subordinate to desk-config.mdc
alwaysApply: true
---

# BTC filters — policy memory (bitget-bot)

**Live desk numbers live in `.cursor/rules/desk-config.mdc`.** That file wins over this one, over v1, and over prior chats.

## Source of truth
- Desk config: `.cursor/rules/desk-config.mdc` (user 2026-09-16)
- Reference tree `C:\divergent` is read-only context — do not copy its RSI/TF/daily-loss defaults onto this desk
- Regime TF: **4h** (same as v1 live DB). 1h (2026-09-10) split the books and is retired unless the user asks

## Applied on bitget (desk-config, 2026-09-16)
- Both sides: `ALLOW_LONG=1`, `ALLOW_SHORT=1`
- `ALLOWED_TYPES=BULLISH_DIV,BEARISH_DIV,LEVEL_CROSS_DOWN` (no `LEVEL_CROSS_UP`)
- `RSI_LONG_MAX=0` (off — not v1’s 30)
- Full symmetric regime: bearish blocks long, bullish blocks short
- `BTC_REGIME_TF=4h`
- `BTC_REGIME_ENABLED=1`
- `BTC_EMA50_FILTER_ENABLED=0`
- `BTC_MOMENTUM_PCT=1.2`, `BTC_MOMENTUM_HOURS=4`
- Pair blocker live: `BLOCK_CONSECUTIVE_LOSSES=3`, `BLOCK_MIN_TRADES=4`, WR 30 / cooldown 48h
- **TF blocker off** (`TF_BLOCKER_ENABLED=0`). Book scans `15m,1h,4h`. Do not re-enable the TF ban
- `MAX_POSITIONS=15`, `MAX_SAME_DIRECTION_POSITIONS=4`
- Dollar stop: `MAX_LOSS_PCT_OF_MARGIN=40`, `DOLLAR_STOP_SL_PCT_THRESHOLD=0`
- Daily kill: `MAX_DAILY_LOSS_USD=150`

## Keep for now
1. Log `btc_filter` reasons in decisions
2. Do not copy v1 `max_open_positions=60` or `rsi_long_max=30`
3. Do not restore `BTC_REGIME_TF=1h` unless the user asks
4. Do not restore `RSI_LONG_MAX=30` — signal-bar RSI on bullish div is almost always >30; the user turned the zone off
5. Price sanity lives in `desk-config.mdc` (rToken open 2.5%, exit vs entry 3%) — do not widen back to crypto 5% on stocks
